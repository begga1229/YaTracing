{
  "name": "yatracing",
  "version": "1.0.0",
  "description": "Construction Tracking Application - Web, Mobile & Backend",
  "private": true,
  "workspaces": [
    "packages/backend",
    "packages/web",
    "packages/mobile"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=packages/backend\" \"npm run dev --workspace=packages/web\"",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "install-deps": "npm install && npm install --workspace=packages/backend && npm install --workspace=packages/web && npm install --workspace=packages/mobile"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}